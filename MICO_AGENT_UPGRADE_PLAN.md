# Mico 高标准 Agent 升级计划

## 0. 项目北极星与不可突破的边界

**目标产品**：一个面向微生态科研的、可复现且可审计的分析 Agent。它基于标准化宏基因组数据构建队列、运行物种差异分析、核验文献证据，并生成带数据来源与局限性的研究报告。

**对外定位**：科研分析与决策支持，不是临床诊断或治疗系统。

**数据规模表述**：远程业务库当前有 24,264 条内部业务记录；其中标准丰度表有 23,072 个不同的 `(patient_id, sample_id)` 样本键和 20,852,714 条标准化物种丰度存储行。13,897 个 Meta2DB 样本均能匹配到标准丰度表。除非后续能证明唯一受试者关系，任何页面、接口、报告和简历都不得把 24,264 条记录称作 24,264 位独立患者；任何物种谱覆盖率说明都必须使用 23,072 个标准丰度样本键这一口径。

**事实源边界**：

- Java 主业务系统和远程 MySQL 是业务事实源；Agent 只能通过受控业务 API / MCP 工具取数，不能直连或绕开业务 API 改查数据库。
- Python 统计服务只执行确定性统计与模型推理；不生成医学事实。
- LLM 只做规划、工具选择、证据综合和自然语言表达；不能编造数据、疾病映射或统计结果。
- 外部文献仅作为外部证据，不能覆盖内部数据事实。

**运行时决策**：Agent 编排运行时确定采用 **Python + LangGraph**。Java 后端、现有网页、权限和远程 MySQL 不迁移、不替换；Python 服务仅通过稳定的 Java 业务 API / MCP 工具访问事实与提交计算任务。

## 1. 目标架构

```text
Web / Java 业务系统（事实源、权限、稳定 API）
                     |
                     v
Python Mico Agent Runtime（任务图、状态、审批、Trace、评测）
       |                 |                 |
       v                 v                 v
  Mico Data MCP     统计计算服务       文献/知识检索
       |                 |                 |
       +------ 受控、强类型、可审计工具 ------+
                     |
                     v
远程 MySQL（业务数据）/ Agent 审计库 / 可复现分析产物库
```

Agent Runtime 采用 Python + LangGraph，并吸收 Ops 的工程能力：持久化工作流、条件路由、工具白名单、调用预算、审批、队列、重试、SSE、Trace 和 fixture 驱动评测。

Mico 的差异化能力是：领域本体、物种级数据、可复现统计、证据正反核验和严格的医学安全边界。

## 2. 分阶段实施计划

### 阶段 1：领域语义与数据契约（先做）

**目的**：消除“项目、样本、受试者、患者”混用以及疾病标签重复问题，给所有后续 Agent 工具提供稳定输入输出。

**工作内容**：

1. 审计现有 MySQL 表、Java DTO、Mapper 和前端字段，输出字段级事实映射。
2. 固化五个实体及关系：`Study`、`Subject`、`Sample`、`TaxonomicProfile`、`DiseaseAssertion`。
3. 明确显示规则：例如 `SRR1518476` 是样本名，`2015_Castro-NallarE` 是项目名；内部 `patient_id` 只是当前业务记录主键，不自动等同真实患者。
4. 定义疾病标准化契约：`raw_label -> canonical_disease_id -> canonical_name_zh -> mapping_status -> mapping_version`；历史原始值必须可追溯，LLM 不得自动修改正式映射。
5. 定义分析数据快照字段：数据源、导入批次、疾病映射版本、taxonomy 版本、队列条件、查询哈希、行数、生成时间。
6. 写出数据质量规则与待核验项；本阶段不改远程库、不重导数据、不改页面功能。

**交付物**：

- `mico_database_new/docs/agent-foundation/domain-data-contract-v1.md`
- `mico_database_new/docs/agent-foundation/domain-field-mapping-v1.csv`
- `mico_database_new/docs/agent-foundation/data-quality-baseline-v1.md`
- `mico_database_new/docs/agent-foundation/phase-1-audit-report.md`

**验收门槛**：

- 能用真实样例证明样本名、项目名、内部记录 ID 的区别。
- 130 个当前疾病名称被明确标为“标准化名称集合”，而非未经审核的临床疾病类别数。
- 任何后续工具所需的输入、输出、版本字段均有文档定义。

### 阶段 2：受控的数据与计算工具层

**目的**：把直接查表变为一组可授权、可复现、强类型的 Agent 工具。

**工作内容**：

1. 在 Java 业务服务上提供只读、版本化的能力接口：查样本、查项目、建队列、取物种丰度摘要、取疾病映射、获取数据快照。
2. 定义 MCP/HTTP 工具 JSON Schema；禁止不受约束的自由文本 SQL 和任意表扫描。需要动态科研读取时，只允许模型提出单条、限行、只读 SQL 草案，由 Java 统一执行安全校验与权限边界。
3. 为每个工具添加角色、数据范围、最大返回量、超时、调用预算和审计字段。
4. 将耗时统计任务提交到计算队列，返回 `analysisJobId`，而不是在一次 HTTP 请求中阻塞运行。

**验收门槛**：

- Agent 无数据库账号也能完成所有允许的只读数据访问。
- 每次工具调用可复现：有参数、数据快照、结果行数和 Trace ID。
- 越权参数、过大查询和不受约束的自由 SQL 均被拒绝。

### 阶段 3：Mico Agent Runtime（吸收 Ops 底座）

**目的**：建立可靠的长任务运行时，而不是把流程写成一次性聊天调用。

**工作内容**：

1. 从 Ops 提炼通用模块：LangGraph 图状态、checkpoint、任务队列、SSE、条件重试、工具治理、任务预算、审批、审计日志。
2. 创建独立 Python `mico-agent-runtime` 服务；Java 仍为事实源和主业务入口，保持现有 Controller、Service、Mapper、前端和远程 MySQL 的职责不变。
3. 建立 `agent_run`、`agent_step`、`agent_artifact`、`approval_ticket`、`tool_audit` 等持久化模型。
4. 支持暂停、审批、恢复、取消、失败重试与完整回放。

**服务调用约束**：

```text
前端 -> Java 业务后端 -> Python LangGraph Runtime -> Java 受控 API / MCP -> 远程 MySQL
                                  |
                                  +-> Python 统计任务 / 知识检索
```

- Python Runtime 不持有业务数据库写权限，也不直接执行 SQL；动态 SQL 草案只能经 Java 只读策略最终校验和执行。
- Java 不承担 LLM 循环、图状态或多 Agent 编排；它提供版本化业务能力和访问控制。
- 第一版可由 Java 反向代理或前端直接调用 Runtime；接口归属将在阶段 2 契约中确定。

**验收门槛**：

- 运行过程重启后可从最近成功步骤恢复。
- 导出、外部访问、敏感样本级操作能进入人工审批并恢复。
- 前端可看到阶段进度和失败原因，而不是只看到“思考中”。

### 阶段 4：第一个端到端科研分析工作流

**目的**：做出一个足以成为项目主 Demo 的真实闭环。

**首个问题**：

> 比较 2 型糖尿病队列与年龄、性别、地区尽量匹配的健康对照，哪些物种存在显著差异？结果是否有文献支持？有哪些混杂因素和局限？

**图节点**：意图与安全检查 → 疾病/队列标准化 → 可行性审查 → 队列物化 → 统计计算 → 证据检索（支持与反证）→ 审阅 → 结构化报告。

**统计产物要求**：明确纳排标准、样本数、缺失情况、丰度转换、统计检验、效应量、FDR、多重比较校正和代码/参数版本。

**验收门槛**：

- 结论中每个物种都能回溯到统计产物和具体数据快照。
- 文献证据与内部统计分层显示。
- Agent 不得把相关性写为因果或诊断。

### 阶段 5：证据、记忆与多 Agent 协作

**目的**：让复杂性带来真实增益，而非做多 Agent 展示。

**工作内容**：

1. 逻辑角色划分为队列分析、统计、证据、审阅四个专职 Agent；优先以子图/工具实现，不强制启动四个独立大模型。
2. 内部知识、医学文献、统计结果按来源分层；采用混合检索、重排和反方证据检索。
3. 记忆分为会话任务状态、可复用研究偏好、经过人工验证的知识；不把原始聊天历史当长期医学记忆。
4. 在需要跨服务协作时再引入 A2A；MCP 始终负责工具和数据访问。

**验收门槛**：

- 多 Agent 只在能并行或确有专长差异时调用。
- 检索不到足够证据时明确说明“不足以支持”，而非补造结论。

### 阶段 6：安全、隐私与人工复核

**目的**：使系统适合研究场景，而不越界成无监管医疗诊断。

**工作内容**：

1. 输入、工具、输出三层安全 Gate：诊断/治疗请求、提示注入、越权数据访问、可识别信息导出。
2. 建立风险等级：只读汇总、样本级读取、外部检索、批量导出、写入映射表。
3. 高风险操作一律 fail-closed；记录审批人、理由、原始参数和最终结果。
4. Trace 默认脱敏；用户身份、密钥、完整敏感参数不能进入模型上下文或日志。

**验收门槛**：

- 预设越权和医疗越界测试全部被拦截或安全降级。
- 人工审批拥有可审计的完整闭环。

### 阶段 7：可观测性、评测与回归

**目的**：用数据证明 Agent 可靠，而非凭主观演示。

**工作内容**：

1. Trace 统一记录：计划、Agent、模型、工具、数据快照、统计任务、引用、护栏、审批、耗时和成本。
2. 建立不少于 100 条版本化 Eval：事实准确、疾病别名去重、队列正确性、统计复现、引用支持度、工具策略、拒答、安全攻击和故障恢复。
3. 采用确定性评分优先：数据库/快照断言、JSON Schema、Trace 完整性、策略断言；LLM-as-judge 只做辅助。
4. 建立基线对照：普通 RAG、无 Agent 流程、完整 Mico Agent 的完成度、正确率、成本和延迟。

**验收门槛**：

- P0（编造事实、错误诊断、越权访问）为零。
- 数据版本、工具调用和审批链路完整率 100%。
- 任何 Agent、提示词、工具或知识库改动均自动跑回归。

### 阶段 8：产品化 Demo、文档与简历证据

**目的**：把工程质量让面试官在十分钟内看见。

**工作内容**：

1. 前端增加“分析计划、实时进度、证据链、数据版本、统计产物、局限性、审批状态”视图。
2. 制作三个固定 Demo：队列差异分析、样本资料核验、证据不足时安全拒答。
3. 提供一键演示环境、架构图、数据字典、评测报告和 3–5 分钟录屏。
4. 只在真实测得后填写性能、通过率、成本等量化指标。

## 3. 技术选择原则

- **图运行时**：优先使用 Ops 已验证的 Python + LangGraph 思路；持久化、审批、恢复是硬要求。
- **通信协议**：MCP 用于数据和工具；A2A 是后期可选的 Agent-to-Agent 互操作层，不是第一阶段目标。
- **模型策略**：小模型做分类、路由、格式校验；强模型做复杂规划和报告；关键事实均由工具结果约束。
- **存储策略**：远程 MySQL 保持业务事实源；Agent 运行审计与分析产物必须独立、版本化，不能污染业务表。
- **部署策略**：先以 Docker Compose / 明确服务边界实现稳定可复现，再考虑 Kubernetes；不为“技术栈好看”增加运维复杂度。

## 4. 当前执行顺序

严格按阶段推进。每阶段通过验收门槛后，才开始下一阶段。

当前应执行：**阶段 1：领域语义与数据契约**。
