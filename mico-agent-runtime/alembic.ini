[alembic]
script_location = %(here)s/alembic
prepend_sys_path = .

# Intentionally empty. Migration-only MySQL URL is supplied only through
# MICO_AGENT_RUNTIME_MIGRATION_DATABASE_URL by an approved deployment command.
sqlalchemy.url =
