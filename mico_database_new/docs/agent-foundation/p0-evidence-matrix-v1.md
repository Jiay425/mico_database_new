# Mico P0.1 证据矩阵 v1

核验日期：2026-08-20（Asia/Shanghai）  
远程只读核验记录时间：2026-08-20T18:45:43+08:00  
规则：`evidence_level` 只允许使用 `remote_live_verified`、`local_source_verified`、`source_code_verified`、`historical_baseline_unverified`、`unverified`。远程证据来自临时 SSH 隧道内的 `SHOW CREATE TABLE`、`SHOW INDEX`、`EXPLAIN`、`COUNT`、聚合或限定样例 `SELECT`；隧道已关闭。

| claim | evidence_level | evidence_location | verification_time | current_status | limitation | next_verification_action |
|---|---|---|---|---|---|---|
| 业务 schema 为 `patient_data_manager` | remote_live_verified | 远程 `SELECT DATABASE()` | 2026-08-20T18:45:43+08:00 | 已确认 | 只证明本次隧道所连 schema | P1 将 schema 作为连接健康检查结果返回，不向 Python 暴露连接串 |
| `patients` 总行数为 24,264 | remote_live_verified | 远程 `SELECT COUNT(*) FROM patients`；`SHOW CREATE TABLE`/`SHOW INDEX` | 2026-08-20T18:45:43+08:00 | 已确认；安全表述为宏基因组样本/内部记录行数 | 不证明 24,264 个独立 Subject | P1 返回 `rowCount` 类型和 `subjectLinkStatus`，禁止输出患者人数 |
| `meta2db_sample_metadata` 总行数为 13,897 | remote_live_verified | 远程 `COUNT(*)`；PK `patient_id`、唯一键 `sample_id`；13,897 个 distinct `patient_id` 全部在 `patients` 命中，孤儿数 0 | 2026-08-20T19:06:22+08:00 | 已确认 | 这是 Meta2DB 投影子集，不是全部 `patients` | P1 固化 `sampleKey` 与 `internalRecordId` 的来源说明 |
| `microbe_abundance` 总行数为 3,072,271 | remote_live_verified | 远程 `COUNT(*)`；`SHOW CREATE TABLE`/`SHOW INDEX` | 2026-08-20T18:45:43+08:00 | 已确认 | 未证明与标准丰度表的逐行转换关系 | P1 分开暴露原始与标准 profile 能力，不合并语义 |
| `microbe_abundance_standard` 总行数为 20,852,714 | remote_live_verified | 远程 `COUNT(*)`；`SHOW CREATE TABLE`/`SHOW INDEX` | 2026-08-20T18:45:43+08:00 | 已确认；安全表述为标准化丰度存储行数 | 不证明样本数、患者数或唯一特征数 | P1 快照携带 taxonomy/feature/source batch 和行数类型 |
| `patient_diseases` 总行数为 24,413，`diseases` 总行数为 178 | remote_live_verified | 两表远程 `COUNT(*)`；结构/索引 | 2026-08-20T18:45:43+08:00 | 已确认 | 字典行数不是人工审核本体 | P1 `resolve_disease` 只读返回来源和映射状态 |
| `patient_diseases` 使用 `id` 主键和 `(patient_id,disease_id)` 唯一键，并 FK 到两端 | remote_live_verified | `SHOW CREATE TABLE patient_diseases`、`SHOW INDEX` | 2026-08-20T18:45:43+08:00 | 已确认；孤儿 patient/disease 关联均为 0 | 不能证明 `patients.disease` 字符串与关联表逐 token 完全一致 | P1 增加一致性质量摘要，不静默合并两条查询路径 |
| `SRR1518476` 是样本 accession/样本名 | remote_live_verified | 远程样例：`raw_metadata.run_acc`；`profile_sample` 以其开头；Java `PatientMapper.xml:8-13,39-43` | 2026-08-20T18:45:43+08:00 | 已确认 | `sampleName` 是展示投影，不是 Subject 名 | P1 `resolve_sample` 明确返回 accession、sourceSampleId 和 display name |
| `2015_Castro-NallarE` 是项目/Study 名 | remote_live_verified | 同一远程样例的 `project_name`；本地样本快照佐证 | 2026-08-20T18:45:43+08:00 | 已确认 | 不从复合 `sample_id` 反向猜 Study | P1 单独提供 `resolve_study`，禁止用样本字符串截取代替 |
| 样例内部关联为 `patient_id=18423`；`patient_name`、Meta2DB `sample_id`、`profile_sample` 是同一复合样本标识 | remote_live_verified | 远程限定 JOIN：`patients` ↔ `meta2db_sample_metadata`，`run_acc=SRR1518476` | 2026-08-20T18:45:43+08:00 | 已确认 | `patient_name` 不能据此解释为真实姓名 | P1 API 标签使用 `internalRecordId`/`sourceSampleId`，不使用患者姓名 |
| Meta2DB 13,897 个 `sample_id` 与标准表属于同一命名空间 | remote_live_verified | 远程显式 collation 的索引 `EXISTS` 查询：13,897/13,897 命中且 `patient_id` 一致；标准表 23,072 个 distinct `sample_id` | 2026-08-20T18:45:43+08:00 | 已确认单向覆盖关系 | 两列默认字符集不同；反向全量缺口查询未作为契约前提 | P1 统一比较函数/字符集并增加反向覆盖质量指标 |
| `featureCount` 只能称精确条件下已存储丰度行数 | remote_live_verified | `PatientService.java:77-89,100-106`；远程样例 `patient_id=18423` 为 394 行、394 个 hash/名称、无重复 hash+feature_version 组；表无特征唯一键 | 2026-08-20T18:45:43+08:00 | 已确认保守语义 | 未冻结全表跨版本唯一性和零值规则 | P1 返回 `storedRowCount`，将唯一 feature count 作为独立质量字段 |
| `host_subject_id` 是 Subject 候选，不是已验证 Subject 主键 | remote_live_verified | 远程固定聚合：有效 11,811/13,897；9,603 个候选值；805 个多行候选；最大 42 行；142 个跨项目候选；有效规则排除 NULL、空字符串及 null/unknown/n/a/na/not available/not applicable/missing/none | 2026-08-20T20:55:15+08:00 | 候选可用；`subjectLinkStatus=unverified` | 重复/跨项目不自动证明真实受试者关系；不得从 `patient_id` 推断 Subject | P1 先返回候选和证据，人工/来源规则审核后才允许 Subject 去重 |
| `raw_metadata` 非空数量与 `host_subject_id` 有效覆盖一致 | remote_live_verified | 远程固定聚合：`raw_metadata IS NOT NULL AND TRIM(raw_metadata) <> ''` 为 11,811/13,897；同一缺失规则下 `host_subject_id` 有效值为 11,811，缺失 2,086 | 2026-08-20T20:55:15+08:00 | 已统一台账口径 | 非空 JSON 不自动证明 Subject 字段有效；本次不实现 Subject 去重或真实患者计数 | 后续工具返回原始证据、缺失规则和 `unverified` 状态 |
| `patient_id` 不是 Subject 主键，也不是 Sample accession | source_code_verified | `Patient.java`；`PatientMapper.xml:38-119`；`AgentApiController.java`；远程 `patients` PK | 2026-08-20T18:45:43+08:00 | 领域契约已冻结 | 现有 UI 仍有“患者编号/姓名”标签 | P1 保持兼容字段但增加强类型别名和拒答规则 |
| 当前 130 名称集合来自 Dashboard 规则，不是正式疾病类别数 | remote_live_verified | `DashboardMapper.xml:8-66` 的 `normalizedDiseaseCte`；远程复现 `COUNT(DISTINCT disease)=130`；原始 distinct=315；`diseases`=178 | 2026-08-20T18:45:43+08:00 | 已确认统计口径 | 规则仍包含粗粒度、症状/表型、病原体/感染标签 | P3 人工建立 DiseaseAssertion 映射；P1 返回 `rawLabel + unmapped` |
| 原始疾病输入含控制/健康；130 规则排除六个精确控制 token，但仍含症状/表型、病原体或粗粒度风险 | remote_live_verified | 远程结果含 `neurological`、`diarrhea`、`obese`、`SARS-CoV-2`、`HIV-1`、`cholera` 等；原始精确控制/健康行 7,134；Dashboard 明确排除六类精确 token | 2026-08-20T18:45:43+08:00 | 已确认必须分层 | 其他组合/非标准 token 仍需人工分类 | P3 冻结分类与人工审核状态 |
| 本地样本/丰度统计与远程实时统计不是同一版本 | local_source_verified | `data/meta2db/metadata/meta2db_metadata.csv`；`model/meta2db_species/meta2db_species_summary.json`；本地长表 | 2026-08-20T18:45:43+08:00 | 已分层：本地 13,897 样本、16,241,028 物种行；远程 24,264/20,852,714 | 本地 33,313 特征与远程 40,266 hash 不可混用 | P1 快照强制返回 `dataSource`、`importBatch`、`taxonomyVersion`、`generatedAt` |
| 24,264 与 20,852,714 曾是方案历史基线 | historical_baseline_unverified | `MICO_AGENT_UPGRADE_PLAN.md:9`、`MICO_LANGGRAPH_COLLABORATION_PLAN.md` | 2026-08-20T18:45:43+08:00 | 历史来源本身未提供生成查询；数值已被本次远程 COUNT 复核 | 历史批次/生成时点仍未由方案文件证明 | 将历史文本只作引用，正式工具以远程快照为准 |
| Python LangGraph Runtime 不能直连业务 MySQL | source_code_verified | `MICO_LANGGRAPH_COLLABORATION_PLAN.md`、`MICO_AGENT_UPGRADE_PLAN.md`、`ARCHITECTURE-AGENT.md` | 2026-08-20T18:45:43+08:00 | 架构边界已冻结 | P0 未实现 Runtime | P1 Java API/MCP 先定义权限、上限、审计和快照协议 |
| DiseaseAssertion 映射本阶段不写库 | source_code_verified | P0 交付范围、疾病契约字段及审计报告 | 2026-08-20T18:45:43+08:00 | 仅设计契约；无映射写入 | `diseases`/`patient_diseases` 是现有字典/关联，不是新映射资产 | P3 建立人工审核资产和版本后再接入正式映射 |
